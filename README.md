This repository contains the source for the Kalikoutsi landing site — a static HTML/CSS site with no build step, deployed via GitHub Pages, introducing a small studio and its first app, GymTracker, a privacy-first gym tracking app that keeps all workout data on the user's device.
